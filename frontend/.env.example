# API base URL - REQUIRED for production (Vercel frontend must reach backend)
# Leave empty in dev - Vite proxy forwards /api to backend
# In production: your backend URL, e.g. https://your-api.onrender.com
VITE_API_URL=
